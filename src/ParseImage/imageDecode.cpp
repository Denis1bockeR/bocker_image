#include "imageDecode.h"

ImageDecoder::ImageDecoder(const std::string& puth)
{
	/*std::ifstream file;
	file.open(puth, std::ios::in | std::ios::binary | std::ios::ate);

	if (puth.substr(puth.length() - 3, 3) == "png")
		pngParser();
	else if (puth.substr(puth.length() - 3, 3) == "bmp")
		bmpParser(puth);
	else if (puth.substr(puth.length() - 3, 3) == "jpg")
		jpgParser();
	else
		throw("Error extension image file");*/
}